# Dependencias del backend. Versiones fijadas para que Render instale
# exactamente lo mismo que probamos en local.

flask==3.1.3            # servidor web: expone el endpoint POST /buscar
requests==2.34.2        # peticiones HTTP: consulta la API de las aerolineas y la de Groq
python-dotenv==1.2.3    # lee las claves del archivo .env en local
gunicorn==26.2.0        # servidor de produccion, lo usa Render para arrancar la app
